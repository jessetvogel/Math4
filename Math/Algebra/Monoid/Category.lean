import Math.Algebra.Monoid.Basic
import Math.CategoryTheory.Category

open CategoryTheory

namespace Algebra

-- Unfortunately, this will be a bundled category..

-- def CatMonoid : Category := {
--   obj := Monoid,
--   hom := MonoidHom,
--   id := MonoidHom.id,
--   comp := MonoidHom.comp,
--   comp_id := λ _ => by rfl,
--   id_comp := λ _ => by rfl,
--   comp_assoc := λ _ _ _ => by rfl,
-- }

end Algebra
